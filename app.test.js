const assert = require("assert");
const url = require("url");
// host testing
const mocha = require("mocha");

describe("app", () => {
  it("app", () => {
    console.log("app is started without any bug");
  });
});
